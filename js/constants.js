BLOCK = 16; // Size of one block in the game.
MULT = 3;   // Current scale multiplier.
            // Also update main.scss if changing scale.

ANIM_LENGTH = 150;     // Player movement animation length.
                       // Also update main.scss if changing animation length.
ANIM_LENGTH_NPC = 300; // NPC movement animation length.
                       // Also update main.scss if changing animation length.

// Game width & height should always be odd so player is visually centered.
GAME_WIDTH = 11;  // Width of game view.
GAME_HEIGHT = 11; // Height of game view.